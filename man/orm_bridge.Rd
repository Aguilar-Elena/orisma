% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/orm_bridge.R
\name{orm_bridge}
\alias{orm_bridge}
\title{Bridge Article Detection and Priority Ranking}
\usage{
orm_bridge(
  mx,
  text_col = "abstract",
  lang = getOption("orisma.lang", "en"),
  verbose = getOption("orisma.verbose", TRUE)
)
}
\arguments{
\item{mx}{An \code{orisma_matrix} object, ideally after running \code{\link[=orm_ass]{orm_ass()}}.}

\item{text_col}{Character. Text field to analyse. Default \code{"abstract"}.}

\item{lang}{Character. \code{"en"} or \code{"es"}.}

\item{verbose}{Logical.}
}
\value{
The \code{orisma_matrix} object with added columns:
\code{bridge_score} (0-5), \code{bridge_type} (Strong/Partial/Technical),
\code{bridge_criteria} (which criteria were met).
}
\description{
\code{orm_bridge()} identifies \strong{bridge articles} - studies that connect
technical science with real occupational prevention. These are the
highest-value articles for an occupational health practitioner because
they have already done the translation from laboratory to workplace.

A bridge article simultaneously mentions:
\enumerate{
\item \strong{Technology/process} (what was studied)
\item \strong{Hazardous agent} (what risk was characterised)
\item \strong{Workers} (real people in real workplaces)
\item \strong{Exposure measurement} (quantitative data)
\item \strong{Prevention/recommendation} (actionable output)
}

Articles meeting 4 or 5 criteria are classified as \strong{strong bridges}.
Articles meeting 3 criteria (must include workers + measurement) are
\strong{partial bridges}. Others are technical studies.
}
