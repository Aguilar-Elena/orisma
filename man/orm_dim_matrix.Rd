% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/orm_autodim.R
\name{orm_dim_matrix}
\alias{orm_dim_matrix}
\title{Build a risk category x dimension cross-matrix}
\usage{
orm_dim_matrix(
  result,
  dims,
  min_records = 2L,
  out_dir = NULL,
  filename = "risk_dimension_heatmap.png",
  lang = getOption("orisma.lang", "en"),
  verbose = getOption("orisma.verbose", TRUE)
)
}
\arguments{
\item{result}{An \code{orisma_result} object from \code{\link[=orm_analyse]{orm_analyse()}} or \code{\link[=orm_run]{orm_run()}}.}

\item{dims}{An \code{orisma_dims} object from \code{\link[=orm_autodim]{orm_autodim()}}.}

\item{min_records}{Integer. Min records for a risk category row. Default \code{2}.}

\item{out_dir}{Character or NULL. Directory to save the heatmap PNG.}

\item{filename}{Character. Output filename. Default \code{"risk_dimension_heatmap.png"}.}

\item{lang}{Character. \code{"en"} or \code{"es"}.}

\item{verbose}{Logical.}
}
\value{
Invisibly returns the cross-matrix (risk categories x dimensions).
}
\description{
Builds a risk category x dimension cross-matrix and saves a hierarchical
clustered heatmap with dendrograms and numeric values in each cell.

When \code{dims} was built with \code{method = "blocks"}, the matrix shows
risk categories x normative blocks (A-Safety, B-Hygiene, etc.).
When \code{dims} was built with \code{method = "text"}, the matrix shows
risk categories x discovered text dimensions.
}
