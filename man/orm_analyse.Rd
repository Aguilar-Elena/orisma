% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/orm_analyse.R
\name{orm_analyse}
\alias{orm_analyse}
\title{Compute ORISMA bibliometric indicators and analyses}
\usage{
orm_analyse(
  mx,
  material_col = NULL,
  year_col = "year",
  lang = getOption("orisma.lang", "en"),
  verbose = getOption("orisma.verbose", TRUE)
)
}
\arguments{
\item{mx}{An \code{orisma_matrix} object returned by \code{\link[=orm_extract]{orm_extract()}}.}

\item{material_col}{Character. Name of the column containing material
information. If \code{NULL} (default), MGP is skipped with a warning.}

\item{year_col}{Character. Column name for publication year. Default \code{"year"}.}

\item{lang}{Character. \code{"en"} or \code{"es"}.}

\item{verbose}{Logical. Print progress?}
}
\value{
A list (class \code{orisma_result}) with all indicators and analysis
objects ready for \code{\link[=orm_report]{orm_report()}} and visualisation functions.
}
\description{
\code{orm_analyse()} takes an extraction matrix and computes:
\itemize{
\item \strong{WRDI} - Worker-Risk Disconnection Index: the proportion of studies that
characterise a risk without measuring direct worker exposure. A WRDI of 1
means all studies are purely technical (no worker data); 0 means all
studies include direct worker exposure measurement.
\item \strong{RCS} - Risk Category Saturation Index: relative dominance of each
risk category compared to a uniform-distribution baseline. RCS > 1 means
the category is over-represented; RCS < 1 means it is under-represented.
\item \strong{MGP} - Material-Gap Profile: ratio of a material's known hazard
potential (from the literature consensus) to its proportional coverage in
the corpus. Detects hazardous materials that are academically under-studied.
}

It also computes co-occurrence matrices, temporal trends, and author networks
for visualisation.
}
\examples{
\dontrun{
refs    <- orm_load("my_references/")
deduped <- orm_dedup(refs)
mx      <- orm_extract(deduped)
result  <- orm_analyse(mx)

# View the three core indicators
result$indicators

# View WRDI
result$WRDI
}

}
