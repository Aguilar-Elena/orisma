% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/orm_load.R
\name{orm_load}
\alias{orm_load}
\title{Load bibliographic references from one or multiple files / folders}
\usage{
orm_load(
  path,
  lang = getOption("orisma.lang", "en"),
  verbose = getOption("orisma.verbose", TRUE)
)
}
\arguments{
\item{path}{Character. Path to a \strong{folder} containing reference files, or a
\strong{character vector} of individual file paths.}

\item{lang}{Character. Language for console messages: \code{"en"} (default) or
\code{"es"}. Overrides \code{getOption("orisma.lang")}.}

\item{verbose}{Logical. Print progress messages? Default \code{TRUE}.}
}
\value{
A tibble (class \code{orisma_refs}) with standardised columns:
\describe{
\item{\code{record_id}}{Internal unique identifier assigned by ORISMA}
\item{\code{source_file}}{Name of the original file}
\item{\code{source_db}}{Database inferred from file name or format}
\item{\code{title}}{Article title}
\item{\code{authors}}{Authors (semicolon-separated)}
\item{\code{year}}{Publication year}
\item{\code{doi}}{Digital Object Identifier (if available)}
\item{\code{abstract}}{Abstract text}
\item{\code{keywords}}{Author keywords}
\item{\code{journal}}{Journal name}
\item{\code{volume}, \code{issue}, \code{pages}}{Bibliographic location}
\item{\code{document_type}}{Article, review, conference paper, etc.}
}
}
\description{
\code{orm_load()} is the entry point of every ORISMA analysis. It reads
bibliographic files in \strong{RIS}, \strong{BibTeX}, or \strong{CSV} format from a folder
(or a vector of individual file paths), detects the format of each file
automatically, combines all records into a single tidy data frame, and
records the source database for each record.

All major bibliographic databases export to at least one supported format:\tabular{lll}{
   Database \tab Recommended format \tab Notes \cr
   Web of Science \tab RIS / Plain text \tab Max 1 000 records per batch \cr
   Scopus \tab RIS or CSV \tab Max 2 000 records per batch \cr
   PubMed \tab RIS \tab No limit \cr
   Dimensions \tab CSV or RIS \tab Max 2 500 per batch \cr
   EBSCO (CINAHL, BSC) \tab RIS \tab Up to 25 000 \cr
   ProQuest \tab RIS or BibTeX \tab Max 100 per batch \cr
   Cochrane Library \tab RIS \tab No limit \cr
   Ovid / MEDLINE \tab RIS \tab Max 1 000 per batch \cr
   ScienceDirect \tab RIS \tab No limit \cr
   The Lens (free) \tab RIS or CSV \tab No limit \cr
}
}
\examples{
\dontrun{
# Load all .ris and .bib files from a folder
refs <- orm_load("my_references/")

# Load specific files
refs <- orm_load(c("wos_results.ris", "scopus_results.csv"))

# Spanish messages
refs <- orm_load("mis_referencias/", lang = "es")
}

}
