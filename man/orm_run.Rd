% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/orm_run.R
\name{orm_run}
\alias{orm_run}
\title{Run the complete ORISMA pipeline in one call}
\usage{
orm_run(
  refs,
  dict = orm_dict(),
  topic = NULL,
  autodim_method = "blocks",
  material_col = NULL,
  year_col = "year",
  fuzzy_threshold = 0.9,
  fields = c("title", "abstract", "keywords"),
  lang = getOption("orisma.lang", "en"),
  verbose = getOption("orisma.verbose", TRUE),
  save_report = FALSE,
  out_dir = getOption("orisma.out_dir", "orisma_output")
)
}
\arguments{
\item{refs}{An \code{orisma_refs} object from \code{\link[=orm_load]{orm_load()}}.}

\item{dict}{An \code{orisma_dict} object. Default: \code{\link[=orm_dict]{orm_dict()}}.}

\item{topic}{Character. Domain or technology being analysed (e.g.
'Noise in construction', 'Metal AM'). Used in plot subtitles and
report headers. If NULL, neutral generic text is used.}

\item{autodim_method}{Character. \code{"blocks"} (default) or \code{"text"}.}

\item{material_col}{Character or NULL. Column for MGP. Default NULL.}

\item{year_col}{Character. Year column. Default \code{"year"}.}

\item{fuzzy_threshold}{Numeric. Deduplication threshold. Default \code{0.90}.}

\item{fields}{Character vector. Text fields for extraction. Default
\code{c("title", "abstract", "keywords")}.}

\item{lang}{Character. \code{"en"} or \code{"es"}.}

\item{verbose}{Logical. Default \code{TRUE}.}

\item{save_report}{Logical. Auto-call \code{\link[=orm_report]{orm_report()}}? Default \code{FALSE}.}

\item{out_dir}{Character. Output directory if \code{save_report = TRUE}.}
}
\value{
An \code{orisma_result} object containing all indicators, analyses,
dimensions (\code{result$dims}), extraction matrix (\code{result$mx}),
ASS scores and bridge classification (in \code{result$mx$refs}),
and priority ranking (\code{result$ranking}).
}
\description{
\code{orm_run()} is the \strong{single-function entry point} for a complete ORISMA
analysis. It runs all pipeline steps automatically:
\enumerate{
\item Deduplication (3-step: DOI + title + fuzzy)
\item Risk category extraction (dictionary-based)
\item Bibliometric analysis (WRDI, RCS, MGP indicators)
\item Automatic dimension detection (normative blocks)
\item Abstract Sufficiency Score (ASS, 0-5)
\item Bridge article detection and priority ranking
}
\subsection{Minimal usage (3 lines)}{

\if{html}{\out{<div class="sourceCode r">}}\preformatted{library(orisma)
refs   <- orm_load("my_references/")
result <- orm_run(refs)
orm_report(result, lang = "es")
}\if{html}{\out{</div>}}

All intermediate objects are stored in the result for downstream use
with \code{\link[=orm_report]{orm_report()}}, \code{\link[=orm_risk_sheet]{orm_risk_sheet()}}, \code{\link[=orm_ranking]{orm_ranking()}}, and
\code{\link[=orm_extraction_matrix]{orm_extraction_matrix()}}.
}
}
