% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/orm_risk_sheet.R
\name{orm_risk_sheet}
\alias{orm_risk_sheet}
\title{Generate an occupational risk sheet}
\usage{
orm_risk_sheet(
  result,
  topic = "Occupational risk analysis",
  search_strategy = NULL,
  inclusion_criteria = NULL,
  out_dir = "orisma_output",
  lang = getOption("orisma.lang", "en"),
  min_records = 1L,
  context_rcs_threshold = 15,
  verbose = getOption("orisma.verbose", TRUE)
)
}
\arguments{
\item{result}{An \code{orisma_result} object.}

\item{topic}{Character. Technology or domain being assessed.}

\item{search_strategy}{Character or NULL. Description of the search strategy
used (databases, keywords, date range). If NULL, a placeholder is used.}

\item{inclusion_criteria}{Character or NULL. Description of inclusion/
exclusion criteria applied. If NULL, ORISMA defaults are described.}

\item{out_dir}{Character. Output directory.}

\item{lang}{Character. \code{"en"} or \code{"es"}.}

\item{min_records}{Integer. Min records for a category to appear. Default \code{1}.}

\item{context_rcs_threshold}{Numeric. RCS threshold for context detection.
Default \code{15}.}

\item{verbose}{Logical.}
}
\value{
Invisibly returns the path to the generated HTML risk sheet.
}
\description{
\code{orm_risk_sheet()} generates a structured, actionable risk sheet for
occupational health practitioners. It synthesises ORISMA outputs into
a single HTML document that can be used as supporting evidence in a
workplace risk assessment.

The sheet is \strong{regulation-neutral}: it does not include country-specific
regulations or limit values, as these vary by jurisdiction. The practitioner
applies the relevant national/regional regulation based on the risk
categories identified.

Content:
\itemize{
\item Context analysis (dominant topic of the corpus)
\item Priority traffic light (RED/AMBER/GREEN/GREY) per risk category
\item Evidence summary with confidence level
\item Knowledge gap alerts
\item WRDI interpretation with confidence score
\item Methodological section (bases, deduplication, WRDI definition, limits)
}
}
