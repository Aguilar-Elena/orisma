% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/orm_bridge.R
\name{orm_ranking}
\alias{orm_ranking}
\title{Generate priority reading ranking}
\usage{
orm_ranking(
  mx,
  top_n = 20L,
  out_dir = NULL,
  lang = getOption("orisma.lang", "en")
)
}
\arguments{
\item{mx}{An \code{orisma_matrix} object after running \code{\link[=orm_bridge]{orm_bridge()}} and
optionally \code{\link[=orm_ass]{orm_ass()}}.}

\item{top_n}{Integer. Number of top articles to return. Default \code{20}.}

\item{out_dir}{Character or NULL. Directory to save the ranking CSV.}

\item{lang}{Character. \code{"en"} or \code{"es"}.}
}
\value{
A data frame with the top_n priority articles.
}
\description{
\code{orm_ranking()} produces a \strong{priority reading list} for occupational
health practitioners, ranking articles by their combined relevance score
(bridge score + ASS score + number of risk categories detected).

Articles at the top of the list are those most likely to contain
actionable preventive information and should be read first in full.
}
